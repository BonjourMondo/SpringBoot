<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.energy.simulation.mybatis.dao.UserAuthMapper">
    <select id="findUser" parameterType="String" resultType="com.energy.simulation.mybatis.entity.dto.UserDTO">
        select * from testDB where e_name=#{name}
    </select>
</mapper>